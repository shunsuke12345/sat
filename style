/* ===================================================================
   SAT VOCAB BATTLE ROYALE â€” style.css
   Dark esport / battle-royale aesthetic.  Heavy on keyframe animation:
   glows, pulses, color shifts, shakes.  Nothing static.
   =================================================================== */

/* ---------- Design tokens ---------- */
:root {
  --bg:        #0a0c14;
  --panel:     #11141f;
  --panel-2:   #161a28;
  --line:      #232838;
  --text:      #e9edf7;
  --muted:     #8b93ab;

  --blue:   #00d4ff;
  --purple: #7b2ff7;
  --green:  #1fe086;
  --yellow: #ffd23f;
  --red:    #ff3b5c;
  --gold:   #ffd700;
  --silver: #cfd6e6;
  --bronze: #cd7f32;

  --ring-circ: 339.292; /* 2 * PI * r(54) */
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html, body {
  height: 100%;
  font-family: "Segoe UI", system-ui, -apple-system, Roboto, Helvetica, Arial, sans-serif;
  background: var(--bg);
  color: var(--text);
  overflow: hidden;
  -webkit-tap-highlight-color: transparent;
}

/* ===================================================================
   ANIMATED ATMOSPHERE
   =================================================================== */
.bg-grid {
  position: fixed; inset: 0; z-index: 0; pointer-events: none;
  background-image:
    linear-gradient(rgba(0,212,255,0.05) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,212,255,0.05) 1px, transparent 1px);
  background-size: 44px 44px;
  animation: gridDrift 18s linear infinite;
}
@keyframes gridDrift {
  from { background-position: 0 0, 0 0; }
  to   { background-position: 44px 44px, 44px 44px; }
}
.bg-glow {
  position: fixed; inset: 0; z-index: 0; pointer-events: none;
  background:
    radial-gradient(60vmax 60vmax at 18% 12%, rgba(123,47,247,0.20), transparent 60%),
    radial-gradient(55vmax 55vmax at 85% 88%, rgba(0,212,255,0.16), transparent 60%);
  animation: glowPulse 9s ease-in-out infinite alternate;
}
@keyframes glowPulse {
  from { opacity: 0.65; transform: scale(1); }
  to   { opacity: 1;    transform: scale(1.08); }
}

/* Screen-edge flash on answer */
.edge-flash {
  position: fixed; inset: 0; z-index: 60; pointer-events: none;
  box-shadow: inset 0 0 0 0 transparent; opacity: 0;
}
.edge-flash.flash-green { animation: edgeGreen 0.55s ease-out; }
.edge-flash.flash-red   { animation: edgeRed   0.55s ease-out; }
@keyframes edgeGreen {
  0%   { opacity: 0; box-shadow: inset 0 0 0 0 rgba(31,224,134,0); }
  25%  { opacity: 1; box-shadow: inset 0 0 120px 14px rgba(31,224,134,0.65); }
  100% { opacity: 0; box-shadow: inset 0 0 40px 0 rgba(31,224,134,0); }
}
@keyframes edgeRed {
  0%   { opacity: 0; box-shadow: inset 0 0 0 0 rgba(255,59,92,0); }
  25%  { opacity: 1; box-shadow: inset 0 0 120px 14px rgba(255,59,92,0.7); }
  100% { opacity: 0; box-shadow: inset 0 0 40px 0 rgba(255,59,92,0); }
}

.particle-host { position: fixed; inset: 0; z-index: 200; pointer-events: none; overflow: hidden; }
.fx-layer      { position: fixed; inset: 0; z-index: 210; pointer-events: none; overflow: hidden; }

/* ===================================================================
   GENERIC BUTTONS
   =================================================================== */
.btn-primary {
  position: relative;
  border: none; cursor: pointer;
  padding: 15px 34px;
  font-size: 1.05rem; font-weight: 800; letter-spacing: 1.5px;
  color: #04111a; text-transform: uppercase;
  background: linear-gradient(135deg, var(--blue), #36e0ff 55%, var(--purple));
  border-radius: 12px;
  box-shadow: 0 0 24px rgba(0,212,255,0.45), 0 8px 22px rgba(0,0,0,0.5);
  overflow: hidden;
  transition: transform 0.15s ease, box-shadow 0.2s ease, filter 0.2s ease;
  animation: btnIdle 2.6s ease-in-out infinite;
}
.btn-primary > span { position: relative; z-index: 1; }
.btn-primary::after {
  content: ""; position: absolute; top: 0; left: -120%;
  width: 60%; height: 100%; transform: skewX(-20deg);
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.55), transparent);
  animation: btnSheen 3.2s ease-in-out infinite;
}
@keyframes btnSheen { 0%, 60% { left: -120%; } 100% { left: 130%; } }
@keyframes btnIdle {
  0%,100% { box-shadow: 0 0 22px rgba(0,212,255,0.40), 0 8px 22px rgba(0,0,0,0.5); }
  50%     { box-shadow: 0 0 34px rgba(0,212,255,0.70), 0 8px 22px rgba(0,0,0,0.5); }
}
.btn-primary:hover { transform: translateY(-3px) scale(1.03); filter: brightness(1.1); }
.btn-primary:active { transform: translateY(0) scale(0.98); }
.btn-primary.danger {
  background: linear-gradient(135deg, var(--red), #ff7088 55%, #b51d3a);
  box-shadow: 0 0 24px rgba(255,59,92,0.5), 0 8px 22px rgba(0,0,0,0.5);
  color: #1a0207;
}

/* ===================================================================
   SCREENS / OVERLAYS
   =================================================================== */
.screen, .overlay {
  position: fixed; inset: 0; z-index: 20;
  display: flex; align-items: center; justify-content: center;
  padding: 20px;
}
.overlay { z-index: 100; }
[hidden] { display: none !important; }

/* ---------- START SCREEN ---------- */
.start-screen { z-index: 30; }
.start-card {
  position: relative; z-index: 1;
  width: min(560px, 94vw);
  max-height: 96vh; overflow-y: auto;
  background: linear-gradient(160deg, rgba(22,26,40,0.96), rgba(12,14,22,0.96));
  border: 1px solid var(--line);
  border-radius: 20px;
  padding: 34px 32px 30px;
  text-align: center;
  box-shadow: 0 0 0 1px rgba(0,212,255,0.12), 0 24px 70px rgba(0,0,0,0.7);
  animation: cardRise 0.6s cubic-bezier(.2,.9,.2,1);
}
@keyframes cardRise { from { opacity: 0; transform: translateY(26px) scale(0.97); } to { opacity: 1; transform: none; } }

.big-logo { font-size: 3.2rem; line-height: 1; animation: floaty 3.4s ease-in-out infinite; filter: drop-shadow(0 0 14px rgba(0,212,255,0.6)); }
@keyframes floaty { 0%,100% { transform: translateY(0) rotate(-4deg); } 50% { transform: translateY(-10px) rotate(4deg); } }

.game-title {
  margin-top: 8px; font-size: 3rem; font-weight: 900; letter-spacing: 4px;
  background: linear-gradient(90deg, var(--blue), #fff 50%, var(--purple));
  background-size: 220% auto;
  -webkit-background-clip: text; background-clip: text; color: transparent;
  animation: shimmer 4s linear infinite;
}
.game-subtitle {
  font-size: 1.7rem; font-weight: 900; letter-spacing: 8px; color: var(--red);
  text-shadow: 0 0 18px rgba(255,59,92,0.7);
  animation: pulseGlow 2.2s ease-in-out infinite;
}
@keyframes shimmer { to { background-position: 220% center; } }
@keyframes pulseGlow { 0%,100% { text-shadow: 0 0 12px rgba(255,59,92,0.5); } 50% { text-shadow: 0 0 26px rgba(255,59,92,0.95); } }
.title-underline {
  height: 3px; width: 70%; margin: 14px auto 0;
  background: linear-gradient(90deg, transparent, var(--blue), var(--purple), transparent);
  background-size: 200% auto; animation: shimmer 3s linear infinite;
}
.start-tag { margin: 16px 0 22px; color: var(--muted); font-size: 0.98rem; }

.field { text-align: left; margin: 14px 0; }
.field > label {
  display: block; font-size: 0.72rem; letter-spacing: 2px; color: var(--blue);
  margin-bottom: 7px; font-weight: 700;
}
#usernameInput {
  width: 100%; padding: 13px 16px; font-size: 1rem; color: var(--text);
  background: #0c0f18; border: 1px solid var(--line); border-radius: 10px;
  outline: none; transition: border-color 0.2s, box-shadow 0.2s;
}
#usernameInput:focus { border-color: var(--blue); box-shadow: 0 0 0 3px rgba(0,212,255,0.18); }

.difficulty-pick { display: flex; gap: 10px; }
.diff-btn {
  flex: 1; padding: 11px 6px; cursor: pointer; font-weight: 700; font-size: 0.9rem;
  color: var(--muted); background: #0c0f18; border: 1px solid var(--line);
  border-radius: 10px; transition: all 0.18s ease;
}
.diff-btn:hover { transform: translateY(-2px); color: var(--text); border-color: var(--blue); }
.diff-btn.active {
  color: #04111a; background: linear-gradient(135deg, var(--blue), var(--purple));
  border-color: transparent; box-shadow: 0 0 16px rgba(0,212,255,0.5);
}

.rules { text-align: left; margin: 20px 0 24px; display: grid; gap: 9px; }
.rule { font-size: 0.88rem; color: var(--muted); display: flex; align-items: center; gap: 10px; }
.dot { width: 9px; height: 9px; border-radius: 50%; flex: none; }
.dot.green { background: var(--green); box-shadow: 0 0 10px var(--green); }
.dot.red   { background: var(--red);   box-shadow: 0 0 10px var(--red); }
.dot.blue  { background: var(--blue);  box-shadow: 0 0 10px var(--blue); }
.hint { margin-top: 14px; font-size: 0.78rem; color: var(--muted); opacity: 0.85; }

/* ===================================================================
   GAME LAYOUT
   =================================================================== */
.game-wrap {
  position: fixed; inset: 0; z-index: 10;
  display: grid; grid-template-columns: 1fr 340px;
  gap: 18px; padding: 18px;
}
.main-col { display: flex; flex-direction: column; gap: 16px; min-width: 0; min-height: 0; }

/* ---------- HUD ---------- */
.hud {
  display: flex; justify-content: space-between; align-items: center; gap: 14px;
  background: linear-gradient(135deg, var(--panel), var(--panel-2));
  border: 1px solid var(--line); border-radius: 14px; padding: 12px 16px;
  box-shadow: 0 0 0 1px rgba(123,47,247,0.10);
}
.hud-left { display: flex; align-items: center; gap: 18px; flex-wrap: wrap; }
.player-badge { display: flex; align-items: center; gap: 11px; }
.avatar {
  width: 44px; height: 44px; flex: none; border-radius: 11px;
  display: grid; place-items: center; font-weight: 900; font-size: 1.2rem; color: #04111a;
  background: linear-gradient(135deg, var(--blue), var(--purple));
  box-shadow: 0 0 16px rgba(0,212,255,0.5);
}
.player-name { font-weight: 800; font-size: 1.02rem; }
.player-rank { display: block; font-size: 0.74rem; color: var(--muted); margin-top: 2px; }

.hp-wrap { display: flex; align-items: center; gap: 10px; }
.hp-label { font-size: 0.7rem; letter-spacing: 1.5px; color: var(--muted); font-weight: 700; }
.hp-bar {
  position: relative; width: 190px; max-width: 40vw; height: 20px;
  background: #0b0e16; border: 1px solid var(--line); border-radius: 11px; overflow: hidden;
}
.hp-fill {
  height: 100%; width: 100%; border-radius: 11px;
  background: linear-gradient(90deg, var(--green), #59ffae);
  transition: width 0.5s cubic-bezier(.2,.9,.2,1), background 0.4s ease;
  box-shadow: 0 0 14px rgba(31,224,134,0.6);
}
.hp-fill.mid  { background: linear-gradient(90deg, #d8a400, var(--yellow)); box-shadow: 0 0 14px rgba(255,210,63,0.6); }
.hp-fill.low  { background: linear-gradient(90deg, #b51d3a, var(--red));    box-shadow: 0 0 16px rgba(255,59,92,0.75); }
.hp-text {
  position: absolute; inset: 0; display: grid; place-items: center;
  font-size: 0.72rem; font-weight: 800; color: #04111a; text-shadow: 0 1px 1px rgba(255,255,255,0.3);
}
.hp-wrap.shake { animation: shake 0.45s; }

.hud-right { display: flex; gap: 12px; }
.stat-box {
  background: #0b0e16; border: 1px solid var(--line); border-radius: 11px;
  padding: 7px 16px; text-align: center; min-width: 88px;
}
.stat-label { font-size: 0.64rem; letter-spacing: 1.6px; color: var(--muted); font-weight: 700; }
.stat-value { font-size: 1.45rem; font-weight: 900; line-height: 1.1; }
.streak-value { display: inline-flex; align-items: center; gap: 4px; transition: transform 0.25s; }
.fire { font-size: 1rem; display: none; }
.streak-box.active { border-color: var(--yellow); box-shadow: 0 0 16px rgba(255,210,63,0.4); }

/* Leave-match / home button */
.home-btn {
  display: inline-flex; align-items: center; gap: 7px; align-self: stretch;
  padding: 0 14px; cursor: pointer;
  background: #0b0e16; border: 1px solid var(--line); border-radius: 11px;
  color: var(--muted); font-weight: 800; font-size: 0.72rem; letter-spacing: 1.4px;
  transition: transform 0.16s ease, color 0.18s, border-color 0.18s, box-shadow 0.2s;
}
.home-btn:hover {
  color: var(--red); border-color: var(--red);
  box-shadow: 0 0 14px rgba(255,59,92,0.35); transform: translateY(-2px);
}
.home-btn:active { transform: translateY(0) scale(0.97); }
.home-ico { font-size: 1rem; }
.streak-box.bump .streak-value { animation: streakBump 0.45s ease; }
@keyframes streakBump { 0% { transform: scale(1); } 45% { transform: scale(1.45); } 100% { transform: scale(1); } }

/* ---------- QUESTION CARD ---------- */
.question-card {
  position: relative; flex: 1; min-height: 0;
  background: linear-gradient(160deg, rgba(20,24,38,0.96), rgba(11,13,21,0.97));
  border: 1px solid var(--line); border-radius: 18px;
  padding: 22px 26px 26px; overflow: hidden;
  display: flex; flex-direction: column;
  box-shadow: 0 0 0 1px rgba(0,212,255,0.10), 0 18px 50px rgba(0,0,0,0.5);
  animation: cardRise 0.45s ease;
}
.accent-line {
  position: absolute; top: 0; left: 0; right: 0; height: 4px;
  background: linear-gradient(90deg, var(--blue), var(--purple), var(--red), var(--blue));
  background-size: 300% auto; animation: shimmer 3.5s linear infinite;
}
.q-head { display: flex; justify-content: space-between; align-items: flex-start; gap: 16px; }
.q-type-badge {
  display: inline-block; padding: 7px 14px; border-radius: 999px;
  font-size: 0.72rem; font-weight: 800; letter-spacing: 2px;
  color: var(--blue); background: rgba(0,212,255,0.10);
  border: 1px solid rgba(0,212,255,0.4);
  box-shadow: 0 0 14px rgba(0,212,255,0.25); animation: badgePop 0.4s ease;
}
@keyframes badgePop { from { transform: scale(0.8); opacity: 0; } to { transform: scale(1); opacity: 1; } }

/* Circular timer */
.timer { position: relative; width: 78px; height: 78px; flex: none; }
.timer-svg { width: 100%; height: 100%; transform: rotate(-90deg); }
.ring-bg { fill: none; stroke: #1b2030; stroke-width: 8; }
.ring-fg {
  fill: none; stroke: var(--green); stroke-width: 8; stroke-linecap: round;
  stroke-dasharray: var(--ring-circ); stroke-dashoffset: 0;
  transition: stroke-dashoffset 0.12s linear, stroke 0.4s ease;
  filter: drop-shadow(0 0 6px rgba(31,224,134,0.7));
}
.ring-fg.mid { stroke: var(--yellow); filter: drop-shadow(0 0 6px rgba(255,210,63,0.8)); }
.ring-fg.low { stroke: var(--red);    filter: drop-shadow(0 0 8px rgba(255,59,92,0.9)); }
.timer-num {
  position: absolute; inset: 0; display: grid; place-items: center;
  font-size: 1.55rem; font-weight: 900; color: var(--text);
}
.timer.urgent .timer-num { color: var(--red); animation: timerShake 0.5s infinite; }
@keyframes timerShake {
  0%,100% { transform: translate(0,0) scale(1.08); }
  25% { transform: translate(-2px,1px) scale(1.14); }
  75% { transform: translate(2px,-1px) scale(1.14); }
}

.q-context {
  margin: 16px 0 0; padding: 13px 16px; border-left: 3px solid var(--purple);
  background: rgba(123,47,247,0.10); border-radius: 0 10px 10px 0;
  font-style: italic; color: #d4d9e8; font-size: 0.98rem; animation: cardRise 0.4s ease;
}
.q-context b { color: var(--yellow); font-style: normal; }
.q-prompt {
  margin: 18px 0 4px; font-size: clamp(1.15rem, 2.4vw, 1.7rem); font-weight: 800; line-height: 1.35;
}
.q-prompt .hl {
  color: var(--blue); text-shadow: 0 0 14px rgba(0,212,255,0.5);
  border-bottom: 2px dashed rgba(0,212,255,0.5);
}
.q-prompt .blank {
  display: inline-block; min-width: 90px; border-bottom: 3px solid var(--yellow);
  color: var(--yellow); text-align: center; animation: pulseGlow 1.6s ease-in-out infinite;
}

/* ---------- ANSWERS ---------- */
.answers {
  margin-top: auto; padding-top: 18px;
  display: grid; grid-template-columns: 1fr 1fr; gap: 12px;
}
.answer-btn {
  position: relative; display: flex; align-items: center; gap: 14px;
  padding: 15px 16px; cursor: pointer; text-align: left;
  background: linear-gradient(135deg, #141826, #0e1119);
  border: 1px solid var(--line); border-radius: 13px;
  color: var(--text); font-size: 1rem; font-weight: 600;
  transition: transform 0.14s ease, border-color 0.18s, box-shadow 0.2s, background 0.2s;
  animation: ansIn 0.4s ease backwards;
}
.answer-btn:nth-child(1) { animation-delay: 0.04s; }
.answer-btn:nth-child(2) { animation-delay: 0.10s; }
.answer-btn:nth-child(3) { animation-delay: 0.16s; }
.answer-btn:nth-child(4) { animation-delay: 0.22s; }
@keyframes ansIn { from { opacity: 0; transform: translateY(14px); } to { opacity: 1; transform: none; } }

.answer-btn .badge {
  width: 34px; height: 34px; flex: none; border-radius: 9px;
  display: grid; place-items: center; font-weight: 900; font-size: 1rem;
  background: #1b2233; color: var(--blue); border: 1px solid rgba(0,212,255,0.35);
  transition: all 0.18s ease;
}
.answer-btn .txt { flex: 1; }
.answer-btn:not(.locked):hover {
  transform: translateY(-4px); border-color: var(--blue);
  box-shadow: 0 8px 24px rgba(0,0,0,0.5), 0 0 20px rgba(0,212,255,0.35);
  background: linear-gradient(135deg, #18203a, #0e1119);
}
.answer-btn:not(.locked):hover .badge { background: var(--blue); color: #04111a; box-shadow: 0 0 14px rgba(0,212,255,0.7); }
.answer-btn:not(.locked):active { transform: translateY(-1px) scale(0.99); }
.answers.locked .answer-btn { cursor: default; }

.answer-btn.correct {
  border-color: var(--green); background: linear-gradient(135deg, rgba(31,224,134,0.22), rgba(14,17,25,0.9));
  box-shadow: 0 0 26px rgba(31,224,134,0.55); animation: correctBounce 0.5s ease;
}
.answer-btn.correct .badge { background: var(--green); color: #04140b; border-color: var(--green); }
.answer-btn.correct .txt::after { content: " âœ”"; color: var(--green); font-weight: 900; }
@keyframes correctBounce {
  0% { transform: scale(1); } 30% { transform: scale(1.06); } 55% { transform: scale(0.97); } 100% { transform: scale(1); }
}
.answer-btn.wrong {
  border-color: var(--red); background: linear-gradient(135deg, rgba(255,59,92,0.22), rgba(14,17,25,0.9));
  box-shadow: 0 0 24px rgba(255,59,92,0.5); animation: shake 0.45s;
}
.answer-btn.wrong .badge { background: var(--red); color: #1a0207; border-color: var(--red); }
.answer-btn.wrong .txt::after { content: " âœ˜"; color: var(--red); font-weight: 900; }
.answer-btn.faded { opacity: 0.4; filter: grayscale(0.4); }

@keyframes shake {
  0%,100% { transform: translateX(0); }
  15% { transform: translateX(-9px); } 30% { transform: translateX(8px); }
  45% { transform: translateX(-6px); } 60% { transform: translateX(5px); }
  75% { transform: translateX(-3px); } 90% { transform: translateX(2px); }
}

/* ===================================================================
   LEADERBOARD
   =================================================================== */
.leaderboard {
  background: linear-gradient(160deg, var(--panel), rgba(11,13,21,0.97));
  border: 1px solid var(--line); border-radius: 16px;
  display: flex; flex-direction: column; min-height: 0; overflow: hidden;
  box-shadow: 0 0 0 1px rgba(123,47,247,0.12);
}
.lb-header {
  display: flex; justify-content: space-between; align-items: center;
  padding: 14px 16px 10px; border-bottom: 1px solid var(--line);
}
.lb-header h3 { font-size: 0.92rem; letter-spacing: 2px; }
.lb-alive { font-size: 0.76rem; color: var(--green); font-weight: 700; }
.lb-alive span { color: #fff; }
.lb-cols {
  display: grid; grid-template-columns: 30px 1fr 60px 30px 26px;
  gap: 6px; padding: 8px 14px; font-size: 0.62rem; letter-spacing: 1px;
  color: var(--muted); border-bottom: 1px solid var(--line);
}
.lb-cols span:nth-child(3) { text-align: right; }
.lb-cols span:nth-child(4), .lb-cols span:nth-child(5) { text-align: center; }
.lb-list { flex: 1; overflow-y: auto; padding: 6px; scrollbar-width: thin; }
.lb-list::-webkit-scrollbar { width: 7px; }
.lb-list::-webkit-scrollbar-thumb { background: #2a3146; border-radius: 6px; }

.lb-row {
  display: grid; grid-template-columns: 30px 1fr 60px 30px 26px; align-items: center;
  gap: 6px; padding: 8px 8px; margin: 3px 0; border-radius: 9px;
  background: #0e111a; border: 1px solid transparent; font-size: 0.82rem;
  will-change: transform;
}
.lb-rank { font-weight: 800; color: var(--muted); text-align: center; }
.lb-name { font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.lb-score { text-align: right; font-weight: 800; color: var(--blue); font-variant-numeric: tabular-nums; }
.lb-streak { text-align: center; font-size: 0.72rem; color: var(--yellow); }
.lb-status { text-align: center; font-size: 0.9rem; }

.lb-row.you { border-color: var(--blue); background: linear-gradient(90deg, rgba(0,212,255,0.14), #0e111a); box-shadow: 0 0 14px rgba(0,212,255,0.25); }
.lb-row.you .lb-name { color: var(--blue); }
.lb-row.rank-1 { border-color: rgba(255,215,0,0.55); background: linear-gradient(90deg, rgba(255,215,0,0.14), #0e111a); }
.lb-row.rank-2 { border-color: rgba(207,214,230,0.45); background: linear-gradient(90deg, rgba(207,214,230,0.10), #0e111a); }
.lb-row.rank-3 { border-color: rgba(205,127,50,0.5);   background: linear-gradient(90deg, rgba(205,127,50,0.12), #0e111a); }
.lb-row.rank-1 .lb-rank, .lb-row.rank-2 .lb-rank, .lb-row.rank-3 .lb-rank { color: #fff; }
.crown-ico { animation: crownBob 2s ease-in-out infinite; display: inline-block; }
@keyframes crownBob { 0%,100% { transform: translateY(0); } 50% { transform: translateY(-2px); } }

.lb-row.eliminated { opacity: 0.4; filter: grayscale(0.6); }
.lb-row.eliminated .lb-name { text-decoration: line-through; color: var(--muted); }
.lb-row.just-out { animation: deathFlash 0.7s ease; }
@keyframes deathFlash {
  0% { background: rgba(255,59,92,0.5); transform: scale(1.02); }
  100% { background: #0e111a; transform: scale(1); }
}

/* ===================================================================
   ROUND / COUNTDOWN OVERLAY
   =================================================================== */
.round-overlay {
  background: radial-gradient(circle at center, rgba(10,12,20,0.92), rgba(5,6,11,0.98));
  backdrop-filter: blur(4px);
}
.round-overlay.show { animation: overlayIn 0.4s ease; }
@keyframes overlayIn { from { opacity: 0; } to { opacity: 1; } }
.round-inner { text-align: center; animation: roundSlide 0.5s cubic-bezier(.2,.9,.2,1); }
@keyframes roundSlide { from { opacity: 0; transform: translateY(-40px) scale(0.94); } to { opacity: 1; transform: none; } }
.round-label {
  font-size: clamp(2.6rem, 8vw, 5rem); font-weight: 900; letter-spacing: 6px;
  background: linear-gradient(90deg, var(--blue), #fff, var(--purple));
  background-size: 220% auto; -webkit-background-clip: text; background-clip: text; color: transparent;
  animation: shimmer 3s linear infinite;
}
.round-stats { margin: 18px 0 8px; }
.round-elim { font-size: 1.4rem; font-weight: 800; color: var(--red); text-shadow: 0 0 16px rgba(255,59,92,0.6); }
.round-remain { font-size: 1.1rem; color: var(--muted); margin-top: 6px; }
.round-countdown {
  margin-top: 22px; font-size: clamp(4rem, 16vw, 9rem); font-weight: 900; line-height: 1;
  color: var(--yellow); text-shadow: 0 0 40px rgba(255,210,63,0.7); min-height: 1em;
}
.round-countdown .cd { display: inline-block; animation: cdPop 0.8s ease; }
.round-countdown .go { color: var(--green); text-shadow: 0 0 40px rgba(31,224,134,0.8); }
@keyframes cdPop {
  0% { opacity: 0; transform: scale(2.4); }
  40% { opacity: 1; transform: scale(1); }
  100% { opacity: 0; transform: scale(0.7); }
}

/* ===================================================================
   END SCREENS (VICTORY / DEFEAT)
   =================================================================== */
.end-overlay { backdrop-filter: blur(6px); }
.victory-screen { background: radial-gradient(circle at center, rgba(20,16,4,0.9), rgba(5,6,11,0.98)); }
.defeat-screen  { background: radial-gradient(circle at center, rgba(28,6,12,0.9), rgba(5,6,11,0.98)); }
.end-inner {
  position: relative; z-index: 2; text-align: center;
  width: min(560px, 94vw); padding: 36px 30px;
  background: linear-gradient(160deg, rgba(22,26,40,0.9), rgba(10,12,20,0.94));
  border: 1px solid var(--line); border-radius: 20px;
  box-shadow: 0 24px 70px rgba(0,0,0,0.7); animation: cardRise 0.6s ease;
}
.defeat-screen .end-inner { animation: cardRise 0.6s ease, shake 0.6s ease 0.1s; border-color: rgba(255,59,92,0.4); }
.victory-title {
  font-size: clamp(2.4rem, 7vw, 4rem); font-weight: 900; letter-spacing: 4px; margin-top: 6px;
  color: var(--gold); text-shadow: 0 0 30px rgba(255,215,0,0.8), 0 0 60px rgba(255,215,0,0.4);
  animation: goldGlow 1.8s ease-in-out infinite;
}
@keyframes goldGlow {
  0%,100% { text-shadow: 0 0 24px rgba(255,215,0,0.7), 0 0 50px rgba(255,215,0,0.35); }
  50%     { text-shadow: 0 0 40px rgba(255,215,0,1), 0 0 90px rgba(255,215,0,0.6); }
}
.defeat-title {
  font-size: clamp(2.4rem, 7vw, 4rem); font-weight: 900; letter-spacing: 6px; margin-top: 6px;
  color: var(--red); text-shadow: 0 0 30px rgba(255,59,92,0.9);
  animation: pulseGlow 1.6s ease-in-out infinite;
}
.end-sub { color: var(--muted); margin: 12px 0 22px; }
.end-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 26px; }
.end-stat {
  background: #0c0f18; border: 1px solid var(--line); border-radius: 12px; padding: 14px 8px;
  display: flex; flex-direction: column; gap: 7px;
}
.es-label { font-size: 0.62rem; letter-spacing: 1.4px; color: var(--muted); font-weight: 700; }
.es-val { font-size: 1.55rem; font-weight: 900; color: var(--blue); }
.victory-screen .es-val { color: var(--gold); }
.defeat-screen .es-val  { color: var(--red); }
.crown.big-logo { font-size: 3.4rem; }
.skull.big-logo { animation: floaty 3s ease-in-out infinite; }

/* Confetti + particles */
.confetti-piece {
  position: absolute; top: -20px; width: 10px; height: 16px; border-radius: 2px;
  animation: confettiFall linear forwards;
}
@keyframes confettiFall {
  0%   { transform: translateY(-30px) rotate(0deg); opacity: 1; }
  100% { transform: translateY(105vh) rotate(720deg); opacity: 0.9; }
}
.red-particle {
  position: absolute; width: 8px; height: 8px; border-radius: 50%;
  background: var(--red); box-shadow: 0 0 10px var(--red);
  animation: confettiFall linear forwards;
}

/* ===================================================================
   FLOATING SCORE POPUPS
   =================================================================== */
.score-pop {
  position: absolute; font-weight: 900; font-size: 1.5rem; pointer-events: none;
  text-shadow: 0 2px 10px rgba(0,0,0,0.7); animation: scoreFloat 1.1s ease-out forwards;
  white-space: nowrap;
}
.score-pop.good { color: var(--green); }
.score-pop.bad  { color: var(--red); }
@keyframes scoreFloat {
  0%   { opacity: 0; transform: translateY(0) scale(0.7); }
  15%  { opacity: 1; transform: translateY(-10px) scale(1.15); }
  100% { opacity: 0; transform: translateY(-90px) scale(1); }
}

/* ===================================================================
   RESPONSIVE
   =================================================================== */
@media (max-width: 900px) {
  .game-wrap { grid-template-columns: 1fr; grid-template-rows: auto 1fr auto; overflow-y: auto; }
  .leaderboard { max-height: 40vh; }
  .answers { grid-template-columns: 1fr; }
  .hud { flex-direction: column; align-items: stretch; }
  .hud-right { justify-content: space-between; }
}
@media (max-width: 560px) {
  .game-wrap { padding: 10px; gap: 10px; }
  .question-card { padding: 16px 16px 18px; }
  .q-prompt { font-size: 1.1rem; }
  .timer { width: 64px; height: 64px; }
  .timer-num { font-size: 1.2rem; }
  .hp-bar { width: 150px; }
  .game-title { font-size: 2.1rem; }
  .game-subtitle { font-size: 1.2rem; letter-spacing: 5px; }
  .stat-box { min-width: 70px; padding: 6px 10px; }
  .end-stats { grid-template-columns: 1fr; }
  .lb-cols, .lb-row { grid-template-columns: 26px 1fr 52px 26px 24px; }
}
